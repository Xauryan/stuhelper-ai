# StuHelper AI 本地覆盖层索引

本文件记录同步上游时必须关注的 StuHelper AI 本地覆盖层。格式保持简短：
commit hash + 变更内容 + 同步时保留点。长篇行为说明应放在 commit diff、测试或
专题文档中。

未提交改动先使用 `uncommitted`；提交后必须替换为真实 commit hash。

| Commit                                                          | 范围                           | 变更内容                                                                                                                                                                                                                                                                                                                                                                                             | 同步时保留点                                                                                                                                                                                                                                                           | 参考                               |
| --------------------------------------------------------------- | ------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------- |
| `uncommitted`                                                   | channel 可用性监控 + relay filter worker | 新增基于 `logs` 表的渠道可用性监控聚合接口、classic 监控面板、通道测试主动探测标记，以及 relay request/response/SSE 中间过滤器。                                                                                                                                                                                                         | 保留 `probe` 日志标记、SLA 口径、最近错误独立展示、`RELAY_FILTER_WORKER_*` 配置和日志审计字段；不要退回只看 in-memory telemetry，也不要把过滤器限制在 default 前端。                                                                                                  | `docs/channel/gateway-failover-breaker.md` |
| `e50ab8eb`                                                      | Web/API 访问限制             | 新增 `access_control.*` 配置，支持分别对官网 Web 和 API 启用服务端访问限制；可按中国大陆 IP、欧盟国家 IP、游客、普通用户和管理员分别拦截；地域识别优先使用代理国家代码头，未提供时可读取本地 MaxMind 兼容 MMDB 国家库。                                                                                                                | 同步认证、路由、中间件、系统设置页或部署文档时保留 Web/API 策略分离、默认全关、relay OpenAI 风格错误体、后台 API 普通 JSON 错误体，以及认证完成后按真实用户角色执行用户/管理员限制的语义。                                                                     | `docs/access-control.md`           |
| `5a576ca81`, `1a93aff76`, `285f9fce6`, `b27acff0a`, `72a03dd08` | classic 通知/更新公告          | 旧 `Notice` 升级为多条铃铛通知并移动到仪表盘设置；已读通知不重复弹；原 `console_setting.announcements` 改为更新公告且标题可选；新通知/更新公告按发布时间从旧到新逐条弹出详情；详情弹窗展示相对时间和绝对发布时间；通知聚合栏保持 classic 后台条状列表风格；`/console` 更新公告与铃铛弹窗共用同一时间线组件；完整 HTML 使用 sandbox iframe 展示并允许 iframe 内脚本运行，并用预览容器承载。           | 保留 `allow-scripts`，不得开启 `allow-same-origin`、弹窗或顶层跳转权限；完整 HTML 详情不得贴边直连弹窗头尾；新增或修改通知后用户应重新看到未读提示。                                                                                                                   | 本次 classic 通知改动              |
| `b3b7ee6b`, `e2273669`                                         | classic Toast 提示             | classic 全局 `showInfo`、`showSuccess`、`showWarning`、`showError` 和普通 `showNotice` 调用 Semi Toast 时显式传入 `duration`，并把项目毫秒级 toast 常量转换为 Semi 使用的秒，避免批量模型测试等提示依赖组件库默认关闭行为；模型批量测试期间增加前端运行锁，单模型成功/失败只写入结果表，不再逐条弹出长错误 Toast，只保留批量开始、停止和最终汇总提示。                                                                                                         | 同步上游提示封装、渠道测试弹窗或 Semi UI 版本时保留毫秒到秒的转换、批量测试运行锁和批量模式静默单模型 Toast；不要把批量测试恢复为每批/每模型弹提示，否则 401 等长错误会重新堆叠卡住。                                                                                  | 本次 classic Toast 修复            |
| `b61b2b4a3`                                                     | classic 更新公告 Markdown/HTML | 更新公告时间线正文支持 Markdown 和 HTML 片段渲染；完整 HTML 文档继续用 sandbox iframe 展示；无标题公告可以把 `# 标题` 作为正文标题正常展示。                                                                                                                                                                                                                                                         | 保留标题可选语义；不要再用 CSS 隐藏更新公告正文里的 Markdown 标题；HTML 片段走正文渲染，完整 HTML 仍走受限 iframe。                                                                                                                                                    | 本次 classic 通知修复              |
| `e545f683b`                                                     | classic 仪表盘信息面板         | `/console` 更新公告、常见问答、服务可用性底部信息面板按启用组合动态分配 4 列宽度；只开启更新公告和常见问答时更新公告占 3 列、常见问答占 1 列，右边缘与上方图表区域对齐。                                                                                                                                                                                                                             | 保留 4 列网格总宽；不要让只启用两个面板时右侧留下空列。                                                                                                                                                                                                                | 本次 classic 布局修复              |
| `23551e1ac`                                                     | classic 更新公告行宽           | `/console` 更新公告面板变宽时，时间线条目正文跟随卡片列宽扩展，并保留可读行长上限；铃铛弹窗继续使用紧凑时间线宽度。                                                                                                                                                                                                                                                                                  | 不要恢复全局 `360px` 固定行宽覆盖 dashboard 时间线；弹窗时间线仍需保持紧凑。                                                                                                                                                                                           | 本次 classic 布局修复              |
| `abb924931`                                                     | classic 顶栏主题开关           | 顶栏明暗主题按钮改为 Moyuu AI 主页同类胶囊开关，点击在浅色和深色之间切换；保留自动主题的实际显示状态，并增加太阳/月亮、云层和星星的过渡动效。                                                                                                                                                                                                                                                        | 保留 60x28 顶栏紧凑尺寸、键盘焦点状态、`prefers-reduced-motion` 适配，以及自动主题下按实际主题显示的行为。                                                                                                                                                             | `https://moyuu.cc/` 视觉参考       |
| `4980d1d2e`                                                     | classic 默认产品线             | 唯一前端、系统设置默认值和管理员 UI 默认值保持 classic；classic 顶部导航保留排行榜入口。                                                                                                                                                                                                                                                                                                             | 同步上游时不要重新引入 default 前端或把默认产品线切回 default；不要移除 classic 排行榜入口。                                                                                                                                                                           | `docs/upstream-sync-log.md`        |
| `c6e3a021b`, `0810d834f`                                        | 审计管理员                     | 引入并加固只读 `audit_admin` 角色。                                                                                                                                                                                                                                                                                                                                                                  | 只允许列表、搜索、统计和审计查看；禁止详情敏感字段和所有写操作；Web session 请求时重读角色和状态。                                                                                                                                                                     | commit diff                        |
| `f9ea9b99d`                                                     | classic 账号绑定               | 个人设置页只展示 `/api/status` 中已启用的第三方绑定入口。                                                                                                                                                                                                                                                                                                                                            | 禁用的 GitHub、Discord、OIDC、微信、Telegram、LinuxDO 和自定义 OAuth 入口不得展示给普通用户。                                                                                                                                                                          | commit diff                        |
| `b3b7ee6b`                                                     | 自定义 OAuth                   | 通用 OAuth Token/UserInfo 端点响应会先检查 HTML 或非预期内容，避免 EdgeOne/WAF 人机校验页被当作 JSON 解析并向用户暴露 `invalid character '<'`；错误提示会指向 WAF 或人机校验拦截排查。                                                                                                                                                                                                               | 同步 OAuth provider 时保留对 Token/UserInfo 服务端 API 被 WAF 拦截的显式诊断；Casdoor 等 IdP 的 `/api/login/oauth/access_token` 和 `/api/userinfo` 应允许 StuHelper AI 后端服务端请求绕过人机校验。                                                                     | 本次 OAuth 排查修复                |
| `2ac5e6cd`                                                      | 注册入口状态                   | `/api/status` 暴露 `register_enabled`、`password_register_enabled` 和既有 `password_register`；classic 登录页和顶栏注册按钮同时受全局注册开关与自用模式控制，注册页内部继续按 `password_register` 判断用户名/密码注册方式。                                                                                                                                                                      | 同步上游认证状态字段或登录页入口时，不要让后台关闭新用户注册后仍展示注册入口；不要用 `password_register` 替代全局 `register_enabled`，否则会误隐藏 OAuth/Passkey 等非密码注册方式。                                                                                    | `docs/upstream-sync-log.md`        |
| `27980134d`, `275a7807f`, `f3efff454`, `fa7626cb`, `aa5395dd`    | classic 管理 UI                | 邀请管理和账单管理对齐使用日志、用户管理的 CardPro/CardTable 风格；独立账单页默认显示当天 0 点至当前时刻，顶部显示当前筛选范围内的支付成功金额，并按时间段、用户 ID、用户名、支付方式和订单号拆分筛选；账单列表按 `create_time desc, id desc` 排序，历史回填账单不能因批量插入的新 ID 集中显示；管理表格卡片保留横向滚动条可见性，旧首页 `ScrollList` 样式不得全局隐藏 Semi 组件滚动条。                                                                                             | 保留统一卡片、拆分筛选、默认当天范围、后端 `total_money` 仅汇总 `success` 账单、分页、紧凑模式、统计标签和 `PageLayout.jsx` 中的隐藏 Footer 页面列表；不要恢复按 `id desc` 排序；不要把 `.semi-scrolllist` 隐藏规则扩大为全局规则，表格卡片外层必须允许横向溢出滚动。                                                                                       | `docs/official-cn-payments.md`     |
| `e2273669`                                                     | 自助充值/订阅/退款             | 自助充值二维码上传改为前端解码并只保存二维码内容，展示时重新生成二维码且兼容历史图片值；支付链接如 `https://qr.alipay.com/...` 必须作为二维码内容重新生成二维码，不得作为图片 URL 渲染；系统设置移除“拒绝时默认封禁”，封禁只由账单管理“拒绝”确认弹窗中的显式勾选触发；自助支付支持订阅购买，按 `SelfServeTopUpUnitPrice` 独立计算应付金额并创建待审核账单；自助支付退款要求用户提交退款收款码，管理员手动退款后审批登记系统，退款上限复用官方支付退款预览逻辑；新自助订单号使用 `WX_SS`、`ALIPAY_SS`、`WXSUB_SS`、`ALIPAYSUB_SS`，余额订阅使用 `BALANCE__`；订阅退款按重置周期结算，已结束周期不退、当前周期按未用额度比例退、未来完整周期全额退，任意成功订阅退款都会取消对应订阅实例。 | 同步支付设置、充值页、订阅购买、账单管理和退款流程时保留自助支付独立价格、二维码内容化存储、订阅自助入口、退款收款码、人工退款登记语义、订单号前缀、订阅退款即取消实例以及按重置周期结算订阅退款的策略；不要重新保存上传原图，不要把支付链接当 `<img src>`，不要恢复拒绝默认封禁开关或独立的“拒绝并封禁”按钮，不要让自助方式走易支付价格/手续费或第三方退款 API，不要恢复纯按时间比例扣减订阅退款或部分退款保留订阅。 | `docs/self-serve-topup.md`         |
| `a34333bde`, `75e5caf29`, `abe6a63ea`, `b3b7ee6b`              | 用户排行榜                     | classic 用户排行榜支持消耗/充值排行和多周期统计，并隐藏全站总额和占比；消费榜通过 `/api/rankings/users?metric=tokens\|quota\|calls` 按当前指标在后端计算 Top N 和“我的排名”，避免在 Token Top N 样本内前端二次排序造成额度/调用次数排行失真。                                                                                                                                                        | 排行榜接口和页面不得暴露可反推全站消耗或充值的数据；消费榜切换 Token、消费额度、调用次数时必须让后端按相同指标返回榜单和个人排名，不得只返回单一 Token Top N 后在前端重排。                                                                                            | commit diff                        |
| `437f1674`, `1695d83d`, `a9ee0881`, `b400a0ab`                  | 用户额度调整/账单              | 用户管理编辑用户时，额度“添加”拆分为“充值”和“赠送”，后端额度调整只接受 `recharge`、`gift`、`subtract`、`override`；管理员充值写入 `top_ups` 账单，支付方式内部值为 `admin_add`/`admin`，账单管理展示为“管理员充值”，计入充值排行并支持账单退款扣回额度；管理员充值账单的有效支付金额按原生额度换算美元后乘 `AlipayOfficialUnitPrice`，手续费按 `AlipayOfficialServiceFeePercent` 记录为不可退 `fee`；充值/赠送日志在 `Other.admin_info.operation_type` 记录操作类型，日志详情展示操作管理员和操作类型；赠送只调整余额和管理日志，不计入充值排行。历史“管理员增加用户额度”日志迁移为“管理员充值用户额度”，并按日志 ID 回填 `ADMIN_LEGACY_LOG_{id}` 管理员充值账单；历史账单支付方式 `管理员增加`/`管理员充值` 按管理员充值兼容并迁移为 `admin_add`。账单管理允许编辑管理员充值的额度、有效金额、手续费和手续费比例，也允许转为管理员赠送，转赠送会删除对应账单并同步日志。 | 同步用户管理、充值排行和账单退款时，保留充值/赠送的统计差异；不要恢复 `mode=add` 兼容；不要让赠送进入 `top_ups` 或充值排行；管理员充值退款只做本地记账和额度扣回，不调用第三方支付退款，退款金额只按有效支付金额 `money` 计算且手续费不退；旧日志 `管理员增加用户额度` 必须回填为管理员充值账单，旧账单 `payment_method=管理员增加` 必须继续按管理员充值筛选、展示和退款；已有退款记录的管理员充值账单不可编辑或转赠送。     | 本次管理员充值改造                 |
| `f0b661a5c`                                                     | GHCR 发布                      | GHCR workflow 只面向 release tag 发布版本镜像和 `latest`。                                                                                                                                                                                                                                                                                                                                           | 不发布 `main` 或 commit-SHA 镜像 tag；`latest` 指向最新发布版本。                                                                                                                                                                                                      | `.github/workflows`                |
| `4bda2ce96`, `0d95e3331`, `790d5f7f6`, `2d59ef990`, `f3efff454` | 官方中国支付                   | classic 额度充值和订阅支持官方支付宝、微信；官方密钥不从 `/api/option/` 回显；支付金额按规则进位；支付设置支持按通道独立手续费百分比且不保留全局手续费，账单 `money` 表示有效支付金额、`fee` 表示不可退手续费；钱包页默认进入额度充值；账单页支付方式筛选只保留官方支付宝和微信支付；官方支付宝和微信各自使用秒级订单有效期；移动端微信官方支付创建订单前拦截；微信扫码弹窗隐藏订单号并使用单一二维码框和倒计时；账单列表按秒级有效期把超时官方订单标记为已超时，已超时仍可查询，官方充值和官方订阅订单仍可补单。 | 不要用易支付或支付宝当面付覆盖官方企业接入；支付方式只在完整配置时展示；订阅“推荐”由后台开关控制；账单筛选显示可简化为支付宝/微信，但提交值必须保持官方渠道枚举；微信移动端不应 fallback 到 H5 或创建失败订单；官方支付 `expired` 订单后续仍允许查询补齐或管理员补单；退款、返佣冲回和账单成功金额都按有效支付金额计算，手续费只用于支付网关实收总额。 | `docs/official-cn-payments.md`     |
| `64ef9993`                                                      | Waffo Pancake 支付策略         | Waffo Pancake 不作为本地在线充值通道暴露；后端 `/api/user/topup/info` 过滤历史 `PayMethods` 中的 `waffo_pancake`，classic 支付设置和充值页不再保留 Waffo Pancake 创建订单、询价或展示入口；历史账单仍可识别 `waffo_pancake` 支付方式。                                                                                                                              | 同步上游 Waffo Pancake 支付、订阅、catalog 或 classic/default 设置页改动时不要重新暴露新订单入口；如未来产品决定启用，必须重新评估支付配置、退款、返佣、账单和回调安全，而不是恢复半截入口。                                                                          | `docs/upstream-sync-log.md`        |
| `76c0aaba8`, `3935a1aaf`, `5a72c0e70`, `09efb2a7c`              | 邀请奖励/返佣                  | 邀请奖励支持一次性奖励、首充后解锁、充值和订阅返佣、退款冲销与管理员邀请审计。                                                                                                                                                                                                                                                                                                                       | 保留返佣幂等键、退款回滚、所有注册入口持久化 `users.inviter_id`，以及 classic 邀请管理入口。                                                                                                                                                                           | `docs/external-prs.md`             |
| `f592f6954`, `30949524b`, `1324f6cd`                            | 项目身份和分享元数据           | 项目品牌保持 `StuHelper AI`，组织和作者保持 `Xauryan`；运行中站点的标题、副标题、SEO 描述、关键词、分享图和 favicon 通过 `SystemName`、`SystemSubtitle`、`SEODescription`、`SEOKeywords`、`SEOImage`、`Logo` 配置，并由服务端注入 classic `index.html`、前端运行时同步 meta；`/favicon.ico` 会按配置的 Logo 动态重定向，避免落到默认静态 favicon。                                                                 | 不得重新出现旧项目品牌、旧组织、旧包名、旧镜像名；同步上游首页、HTML 模板或前端构建 public 目录时保留站点元信息配置化、favicon link 注入和 `/favicon.ico` 动态处理，不要重新把 SEO 描述、分享标题、首页主副标题或浏览器网站图标写死。                                                                                 | `docs/site-metadata.md`            |
| `1e880b548`                                                     | 用户鉴权请求头                 | Playground 和 session/access-token 鉴权请求头统一为 `StuHelper-AI-User`；历史 default 前端兜底逻辑已随 default 前端移除。                                                                                                                                                                                                                                                                            | classic 前端请求 `/pg/chat/completions` 时必须发送 `StuHelper-AI-User`；不要恢复旧项目名请求头。                                                                                                                                                                       | 本次 playground 修复               |
| `3bf9d4506`, `b0f4e0815`, `b3483802`                            | API Key 分组                   | classic 前端 API Key 创建/编辑入口中不再单独暴露空令牌分组；未选择分组、旧空分组令牌和空分组保存请求统一按 `auto` 分组处理，并只保留一个 `auto` 选项；令牌列表中 `auto` 不再显示为“智能熔断”；`auto` 令牌支持用户配置分组优先级，实际选路先按令牌 `auto_groups`，再补系统默认 `AutoGroups`。                                                                 | 同步上游 API Key UI、令牌鉴权和渠道选择逻辑时保留空分组归一到 `auto`、令牌级 auto 分组优先级高于系统默认顺序、以及 auto 可用分组为空时返回明确错误的行为，避免重新出现“用户分组”和 `auto` 两个入口。                                                                     | 本次 API Key 分组语义调整          |
| `c35f691c`, `uncommitted`                                        | auto 分组重试                  | `auto` 令牌开启跨分组重试后，当前真实分组返回全局可重试错误时会推进到下一个 `auto` 真实分组；即使 `RetryTimes=0`，普通重试耗尽后仍会按 auto 分组列表尝试后续真实分组。新注册用户的初始 `auto` 令牌默认开启跨分组重试。HTTP `502`、`503` 和 Cloudflare `524` 允许作为跨真实分组网关 failover 候选；渠道 `model_mapping` 左侧请求别名会参与能力表和内存缓存选路。                              | 同步 relay 重试、任务 relay、令牌默认值、渠道能力生成、渠道缓存或 auto 分组选路时，保留“跨分组重试独立于普通同分组 RetryTimes”的语义，并保留 model_mapping 请求别名先参与选路、选中渠道后再映射为上游模型的行为；仍不得绕过固定渠道、显式 skip-retry 和渠道亲和失败不切换保护；普通同分组 retry 继续遵守全局自动重试状态码与 504/524 always-skip，`524` 例外只限 `auto` 跨真实分组。                         | `docs/channel/auto-group-retry.md` |
| `6490fa84`                                                       | 渠道亲和临时错误重试           | 渠道亲和命中且规则要求失败不切换时，新增全局开关允许 `429,500,502-503` 等临时上游错误删除当前亲和缓存并切换渠道重试；`fallback_on_temporary_error` 默认开启，`temporary_error_status_codes` 默认 `429,500,502-503`；业务/认证类错误仍保持亲和不切换。                                                                                                                                                | 同步上游渠道亲和或 relay 重试逻辑时，保留显式 `skip-retry`、指定渠道、无剩余重试次数、全局自动重试状态码和 always-skip 状态码的优先级；即使临时错误列表包含 `504/524`，也必须按全局 always-skip 规则不重试。                                                               | `docs/channel/channel-affinity.md` |
| `8cd3efc1`                                                      | classic-only 产品线            | default 前端、模型部署管理和 io.net 部署客户端已删除；后端嵌入、路由、Docker、Makefile、OpenAPI、classic 菜单和 i18n 均只保留 classic 前端和普通渠道配置。                                                                                                                                                                                                                                           | 同步上游时不要重新引入 `web/default`、`/api/deployments`、模型部署管理页面、io.net 部署设置或自动同步渠道锁定。                                                                                                                                                        | 本次 classic-only 清理             |
| `6337e768c`                                                     | Relay 循环保护                 | relay 请求向上游转发时追加带签名的内部路径头；当请求回到本站时，auto/普通分组选择会排除已在链路中出现过的渠道，若没有其他可用渠道则返回 `508 channel:relay_loop`，避免同站点渠道使用本站 auto API Key 造成递归和 524 风暴。                                                                                                                                                                          | 同步上游 relay、通道测试、通道选择逻辑时保留签名路径头、排除已使用渠道和 508 循环中止语义。                                                                                                                                                                            | `docs/channel/relay-loop-guard.md` |
| `4ae1bdef`, `1267146d`, `6e34e4ea`, `fef1db0c`                  | relay / 计费可靠性             | fix 分支引入自动 failover、流式异常显式上报和 in-memory 渠道熔断；本地修复保持 `RetryTimes=0` 代码默认值，熔断配置在 `.env` 加载后初始化，渠道重新启用时清理 breaker；统一 `RetryClass` 供 retry、auto 分组、渠道亲和、自动禁用和熔断复用；渠道列表暴露只读 `breaker_state` 和最近窗口 `availability`；异步任务 relay 成功和上游失败也会上报 breaker/availability；OpenAI Images 与普通 OpenAI SSE error event 返回真实上游错误或 `channel:stream_interrupted`；StreamStatus 读写使用快照/锁保护；流式 relay 采用 `RELAY_TIMEOUT` 响应头等待 + `STREAMING_TIMEOUT` 响应体空闲的分层超时；主 HTTP relay 响应通过统一 pipeline 收口非 200、stream 识别和状态码映射；订阅成功后结算允许记录超额真实用量；Redis quota DB fallback 只失效缓存，Redis 未启用时不派发 quota cache 增减 goroutine。 | 不要把 `RetryTimes` 硬改为非零默认值；新增 `CHANNEL_BREAKER_*` 配置必须经 `service.InitChannelBreakerConfig()` 读取，`CHANNEL_AVAILABILITY_WINDOW_SECONDS` 必须经 `service.InitChannelAvailabilityConfig()` 读取；所有启用渠道入口都要 reset breaker 和 availability；新增/修改流式 handler 后必须检查 `StreamInterruptionError` 或协议内 error event，避免异常截断静默成功；异步任务 relay 必须继续上报 `ReportRelayResult` 并把失败渠道加入当前 retry exclude set；保持 `STREAM_SCANNER_MAX_BUFFER_MB=128` 默认值；不要把流式长响应重新放回 `http.Client.Timeout` 全体超时；订阅预扣继续严格限额，但成功后 settlement delta 必须完整记账；`GetUserQuota` 不得用 DB 快照覆盖 Redis 原子增减后的缓存；Redis 未启用时不要派发 quota cache 增减 goroutine，包括签到后的异步缓存回填。 | `docs/channel/gateway-failover-breaker.md`, `docs/subscription-billing.md` |
| `49b97e38c`                                                     | 渠道亲和重试                   | 渠道亲和规则匹配但未实际使用亲和缓存渠道时，不应因 `skip_retry_on_failure` 阻止普通 relay 重试；只有真正使用亲和渠道失败，或亲和缓存渠道已禁用且规则要求停止时，才停止切换。                                                                                                                                                                                                                         | 同步上游渠道亲和逻辑时保留“规则命中”和“实际亲和命中”的区别，避免首次无缓存请求被误判为亲和失败。                                                                                                                                                                       | 本次渠道亲和重试修复               |
| `ac50e050c`, `0fa064f47`                                        | classic 页脚                   | 页脚保留“设计与开发由 StuHelper AI”；自定义页脚和默认备案/许可证模板分层；许可证类型展示中文业务名称。                                                                                                                                                                                                                                                                                               | 自定义 `Footer` 为空时才使用默认模板；备案号、许可证号、版权字段为空时不显示；移动端允许换行。                                                                                                                                                                         | commit diff                        |
| `a4369f9d1`, `88927991`, `061780d2`, `81ef283a`                    | classic 构建                   | classic 已迁移到 Rsbuild + React 19，并在入口注入 Semi React 19 adapter；前端依赖安装从 `web` workspace 根执行，`web/package.json` 只保留 `classic` workspace，锁文件为 `web/bun.lock`；Prettier lint 排除 Rsbuild `dist` 产物，并修复迁移后遗留的 classic lint 格式问题；`@lobehub/icons` 保持 v2 系列以匹配当前 `@lobehub/ui 2.x` / `antd 5.x` 依赖线，避免 clean Docker build 拉入上游 default 前端使用的 v5 / antd 6 peer graph。 | 不要恢复 `web/classic/vite.config.js`、`web/classic/bun.lock` 或 React 18/Vite 依赖；同步上游前端 workspace 时不要重新引入 `web/default`；不要让 `dist` 构建产物重新进入 Prettier 检查范围；classic 不应升级到 `@lobehub/icons` v5，除非同时完成 `@lobehub/ui` / `antd` 代际升级并验证 Docker 镜像构建。 | 本次 Rsbuild/React 19 迁移         |
| `c986fd426`                                                     | 前端嵌入                       | Go 前端构建目录使用 `go:embed all:` 嵌入 Rsbuild/Rspack 构建产物以及可能以下划线开头的静态资源。                                                                                                                                                                                                                                                                                                    | 不要退回普通目录 `go:embed`，否则生产环境前端构建产物中的隐藏或下划线资源可能 404。                                                                                                                                                                                    | commit diff                        |

## 维护规则

- 新增本地覆盖层时，在同一提交中更新本文件。
- 如果一项覆盖层后来拆成多个提交，在同一行追加关键 hash，不展开长篇说明。
- 如果某项本地覆盖层被上游吸收或产品决定删除，保留一行记录并标明删除/吸收的
  commit hash，避免后续同步重复判断。
